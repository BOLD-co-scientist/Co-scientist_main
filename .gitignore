__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
.env.local
docker-compose.override.yml
CREDENTIALS.local.md
*.local.md

/state/
# per-user harness root marker (api/tenancy.py); never part of a version
.coscientist-user-root
worktrees/
researcher_data/
!researcher_data/.gitkeep

.pytest_cache/
.ruff_cache/
.DS_Store

tailscale-state
# R17: per-user skills are durable cross-version assets (like memory/results),
# not version-bound harness — keep them out of git so a checkout never swaps them.
.claude/skills/
