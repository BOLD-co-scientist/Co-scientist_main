- Write final deliverables as **Markdown**. Keep them human-readable: clear
  headings, lists, and Markdown tables. Save each to `results/<name>.md` via
  `fs_write_workspace.write`.
- Use LaTeX math notation (`$...$` inline, `$$...$$` display) for equations so
  they render in the UI, but keep the document itself Markdown — do not emit a
  full `\documentclass` LaTeX document in this mode.
- In your report to the human, cite the deliverable paths precisely.
